[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npx drizzle-kit push --force && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
